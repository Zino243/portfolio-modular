<!-- Contenido principal -->
    <main class="flex-1 p-8">
    <div class="flex justify-between items-center mb-6">
        <h2 class="text-2xl font-semibold">Editar Portfolio</h2>
        <div class="flex items-center space-x-4">
        <button class="bg-blue-600 text-white px-4 py-2 rounded-lg shadow hover:bg-blue-700 transition">
            💾 Guardar Cambios
        </button>
        <img src="https://i.pravatar.cc/40" alt="user" class="w-10 h-10 rounded-full border" />
        </div>
    </div>

    <!-- Tarjeta de información personal -->
    <section class="bg-white rounded-xl shadow p-6 max-w-3xl">
        <h3 class="text-lg font-semibold mb-4">Información Personal</h3>

        <form class="space-y-4">
        <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">Nombre</label>
            <input 
            type="text"
            placeholder="Tu nombre completo"
            class="w-full border border-gray-300 rounded-lg px-4 py-2 focus:ring focus:ring-blue-200 focus:outline-none"
            />
        </div>

        <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">Sobre Mí</label>
            <textarea
            placeholder="Descripción sobre ti..."
            rows="5"
            class="w-full border border-gray-300 rounded-lg px-4 py-2 focus:ring focus:ring-blue-200 focus:outline-none resize-none"
            ></textarea>
        </div>
        </form>
    </section>
    </main>